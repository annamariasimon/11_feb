import Foundation
import UIKit

class shopInfo {
    var number: Int
    var name: String
    var shopId: String
    
    init(number: Int, name: String, shopId: String) {
        self.number = number
        self.name = name
        self.shopId = shopId
    }
}

